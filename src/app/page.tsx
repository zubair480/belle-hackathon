import { RecallWorkspace } from "@/features/recall";

/**
 * Mounts Ali's RecallWorkspace (codex/ali-ui-pitch). Zubair owns this file; this edit is the
 * expected final-integration mount and can be taken as-is at merge.
 */
export default function HomePage() {
  return <RecallWorkspace />;
}
